"""Planning-aligned Maze-JEPA repair experiments.

This package contains standalone scripts for the next round of Maze-JEPA
experiments. The scripts deliberately reuse the existing ``hdwm``,
``diagnostics`` and ``scripts.eval`` helpers so new results remain comparable to
the previous report.
"""

